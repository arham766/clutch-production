# --- Unsiloed (document parsing) ---
UNSILOED_API_KEY=your-unsiloed-api-key

# --- Moss (semantic search) ---
# Use the SAME values your LiveKit agent uses, so ingestion targets the index it reads.
MOSS_PROJECT_ID=your-moss-project-id
MOSS_PROJECT_KEY=your-moss-project-key
MOSS_INDEX_NAME=fixalong-printer
MOSS_MODEL_ID=moss-minilm
