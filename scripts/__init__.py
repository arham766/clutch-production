"""Standalone, runnable harnesses (the e2e tests) for the Section-D Limbs.

Each module's `*_mini.py` / `*_e2e.py` boots its Limb with real-shaped mini stand-ins
for the neighbours and a `--check` gate. Run via `python -m scripts.<name> --check`.
"""
